# Embeddings sao regeraveis em poucos minutos; nao versionar (~3 MB por colecao)
embeddings/
resultados/*
# ...exceto a tabela final de cada query, que e o resultado reportado.
!resultados/*_bm25_gemma.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Sistema
.DS_Store

# Fonte bruta da amostra e intermediarios do corpus piloto.
# O que o colega precisa para rodar o notebook e o corpus final
# (corpus_piloto_ipc.tsv), que fica versionado.
amostra_50000.xlsx
corpus_piloto_bruto.tsv
corpus_piloto_bruto_nao_encontradas.tsv
corpus_piloto.tsv
dados/corpus_piloto_bruto.tsv
dados/corpus_piloto_bruto_nao_encontradas.tsv
